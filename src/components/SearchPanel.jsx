import{Search}from"lucide-react";const QUICK=["jeseň","šarkan","ovocie","príbeh čiary","chrobáky","zátišie akryl"];export function SearchPanel({query,setQuery,onSearch,onQuickSearch}){return <section className="search-panel"><h1>Čo dnes učíš?</h1><div className="searchbox"><Search size={22}/><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")onSearch()}}/><button onClick={onSearch}>Nájsť</button></div><div className="quick-actions">{QUICK.map(item=><button key={item} onClick={()=>onQuickSearch(item)}>{item}</button>)}</div></section>}
