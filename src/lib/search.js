export function normalizeText(value){return String(value||"").toLowerCase().replace(/[áä]/g,"a").replace(/č/g,"c").replace(/ď/g,"d").replace(/é/g,"e").replace(/í/g,"i").replace(/[ľĺ]/g,"l").replace(/ň/g,"n").replace(/[óô]/g,"o").replace(/ŕ/g,"r").replace(/š/g,"s").replace(/ť/g,"t").replace(/ú/g,"u").replace(/ý/g,"y").replace(/ž/g,"z").replace(/[.,;:!?]/g," ").replace(/\s+/g," ").trim();}
function projectSearchText(project){return normalizeText([project.title,project.grade,project.month,project.methodSeries,project.area,project.goal,project.theme,project.themeCategory,project.technique,project.duration,project.difficulty,project.materials?.join(" "),project.procedure?.join(" "),project.artists?.map(a=>[a.name,a.works?.join(" "),a.note].join(" ")).join(" "),project.referenceWorks?.map(w=>[w.artist,w.title,w.note].join(" ")).join(" ")].join(" "));}
export function searchProjects(projects,query,filters={}){const q=normalizeText(query);const ignored=new Set(["rocnik","dnes","ucim","chcem","tema","technika","pre","hodina","minut"]);const words=q.split(/\s+/).filter(w=>w.length>1&&!ignored.has(w));return projects.map(project=>{const text=projectSearchText(project);let score=0;for(const word of words)if(text.includes(word))score+=3;if(filters.theme&&project.themeCategory!==filters.theme)score-=100;if(filters.grade&&project.grade!==filters.grade)score-=100;if(filters.technique&&!normalizeText(project.technique).includes(normalizeText(filters.technique)))score-=100;if(filters.methodSeries&&project.methodSeries!==filters.methodSeries)score-=100;return{project,score};}).filter(item=>item.score>-50).sort((a,b)=>b.score-a.score).map(item=>item.project);}
